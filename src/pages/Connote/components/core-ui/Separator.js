import React from 'react';
import {View} from 'react-native';

export default function Separator() {
  return (
    <View
      style={{
        borderTopColor: '#000',
        borderTopWidth: 1,
      }}
    />
  );
}
